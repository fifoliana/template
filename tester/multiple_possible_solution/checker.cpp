#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(void)
{
	ifstream in("input_file");
	ifstream out("myAnswer");
	ifstream brute("correctAnswer");

	int t;

	in >> t;

	for (int i = 0; i < t; ++i)
	{
		// read the input (in >> ...)
		// read the answer (out >> ...)
		// read the brute force solution (brute >> ...)

		// check the propreties that make the answer correct or not

		// if not correct return a non-zero value : return 1, with eventually a message

	}
	return (0);

}